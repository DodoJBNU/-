import { styled } from "styled-components";

export const picture = styled.picture`
  img {
    width: inherit;
  }
  .photo {
    width: 20em;
    height: 13em;
    object-fit: cover;
    border-radius: 10px;
  }
`;
